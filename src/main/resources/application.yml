spring:
#  sql:
#    init.mode: always
  profiles:
    include: oauth
  #  jackson:
  #    serialization:
  #      fail-on-empty-beans: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://${HOST}:3306/db_areyoup?useSSL=false&serverTimezone=Asia/Seoul
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
#        format_sql: true
#        default_batch_fetch_size: 100

jwt:
  secret: ${JWT.SECRET}